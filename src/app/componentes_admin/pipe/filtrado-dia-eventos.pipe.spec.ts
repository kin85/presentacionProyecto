import { FintradoDiaEventosPipe } from './fintrado-dia-eventos.pipe';

describe('FintradoDiaEventosPipe', () => {
  it('create an instance', () => {
    const pipe = new FintradoDiaEventosPipe();
    expect(pipe).toBeTruthy();
  });
});
